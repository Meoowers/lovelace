# Lisp that generates JS

It's just because I want macros and I want to create a svelte inside this lisp and compile this svelte
to javascript

```
nodemon index.js ./compiler/std.lisp --ext js,lisp
```